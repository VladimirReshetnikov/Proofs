import BusyBeaver.BB4.NGramBuilder

namespace SetTheory.BusyBeaver.BB4.Certificates.R05A14Guided

def explicitMids087 : List (NGram.MidWord NGram.HistorySymbol) :=
  [{ left := [{ bit := true, history := [((3 : Fin 4), false), ((0 : Fin 4), false)] }, { bit := false, history := [] }], right := [{ bit := true, history := [((2 : Fin 4), false)] }, { bit := false, history := [] }], center := { bit := false, history := [((3 : Fin 4), true), ((1 : Fin 4), false)] }, state := (0 : Fin 4) }, { left := [{ bit := false, history := [] }, { bit := false, history := [] }], right := [{ bit := false, history := [((3 : Fin 4), true), ((1 : Fin 4), false)] }, { bit := true, history := [((2 : Fin 4), false)] }], center := { bit := false, history := [((0 : Fin 4), false)] }, state := (3 : Fin 4) }, { left := [{ bit := false, history := [((0 : Fin 4), false)] }, { bit := false, history := [] }], right := [{ bit := true, history := [((2 : Fin 4), false)] }, { bit := false, history := [] }], center := { bit := true, history := [((1 : Fin 4), false)] }, state := (3 : Fin 4) }, { left := [{ bit := true, history := [((1 : Fin 4), false)] }, { bit := false, history := [((0 : Fin 4), false)] }], right := [{ bit := false, history := [] }, { bit := false, history := [] }], center := { bit := false, history := [] }, state := (2 : Fin 4) }, { left := [{ bit := false, history := [((0 : Fin 4), false)] }, { bit := false, history := [] }], right := [{ bit := false, history := [] }, { bit := false, history := [] }], center := { bit := false, history := [] }, state := (1 : Fin 4) }, { left := [{ bit := false, history := [] }, { bit := false, history := [] }], right := [{ bit := false, history := [] }, { bit := false, history := [] }], center := { bit := false, history := [] }, state := (0 : Fin 4) }]

end SetTheory.BusyBeaver.BB4.Certificates.R05A14Guided
