import BoundedPAConsistency.Basic
import BoundedPAConsistency.Internal
import BoundedPAConsistency.TermEvaluation

/-!
# Bounded-complexity consistency for PA

This facade exposes the metatheoretic polarity ranks, all-occurrences
restricted proof predicates, external standard-model consistency theorem, and
the higher-hierarchy fixed-point induction bridge and represented coded-term
evaluator for the eventual coded soundness argument.  The internal PA proof
of each arithmetized
bounded-consistency sentence remains future work; see the project README for
the precise boundary and roadmap.
-/
